class Booking:

    def __init__(self,
                 booking_id,
                 passenger_id,
                 flight_no,
                 seats):

        self.booking_id = booking_id
        self.passenger_id = passenger_id
        self.flight_no = flight_no
        self.seats = seats